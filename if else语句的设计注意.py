'''

1.每一个“if 语句”必须包含一个else.

2. 如果这个 else 永远都不应该被执行到,因为它本身没有任何意义,那你必须在
   else 语句后面使用一个叫做 die 的函数,让它打印出错误信息并且死给你看
      
3. “if 语句”的嵌套不要超过 2 层,最好尽量保持只有 1 层。这意味着如果你在 if 里
	边又有了一个 if,那你就需要把第二个 if 移到另一个函数里面。

4. 将“if 组合语句”当做段落来对待,在这种组合的最前面和最后面留一个空行以作区分。

5. 你的布尔测试应该很简单,当它们很复杂的时,将它们的运算放到一
个 变量里,并且为变量取一个好名字。

'''